# 模型运行时质量与边界

本文记录截至 `deed0ae` 已采用的运行时行为，适用于 Planning、Study、Persona、Document、Tavern 及其共享 Provider/Harness。MiniMax-M3 是发现问题的实测模型；这些修复不构成其他模型、平台或全部内容质量的认证。生命周期和身份归属仍以 [Harness 架构](harness-architecture.md) 为准。

## Planning

- `get_study_unit_detail` 每个模型轮次最多 3 次，每次操作总上限仍为 4；工具仍按 provider 调用顺序串行执行。其他工具的限制不变。Python Tool Manifest、TypeScript 与 golden fixture 保持一致，历史 v1 pilot 不改写，当前预算回归使用 v2。
- 详情摘录先限定到修订后 Study Unit 的页范围，再优先匹配来源 Section；没有本地页证据时返回空，不以父 Section 的开头冒充当前单元内容。跨页 chunk 保留真实范围。
- Planning 与 Study 共用的页文本工具严格计算字符预算，包含首个超长 chunk 和段落分隔符。图片工具在分配页码列表前验证并限制页范围。
- 一轮所有工具回执完成后再附加图片。严格提案修复保留已获取的工具证据和图片；页码范围错误提供类型化恢复指引。修复与提交共用章节不变量，不能因恢复而放宽来源范围。
- 修复提示区分应用创建/更新时间戳与学习活动时长。实验中的初始时长提示组合未采用，不能从这一修复推导其已上线。
- 单元数量少不再单独意味着范围过粗。初始提示和运行循环保留“至多两个单元且跨度至少 80 页”的既有判断，稀疏子标题等信号独立保留；估分器删除仅由数量触发的跨度扣分，保留实际宽跨度惩罚。
- `estimate_plan_completion` 的产品名称为“学习单元结构估分”。它检查源页跨度、子标题数量与覆盖元数据，不读取计划活动；分数不能认证目标完成度、事实、人格方法或时长算术。初始提示、结果建议、trace 摘要、provider 描述和设置标签均应表达这个边界。

具体 Prompt 结构见 [Planning Prompt 契约](learning-plan-prompt-contract.md)。

## Study、记忆与引用

- 互动题回复清理保留 Markdown 所需空白。模型显式返回 `interactive_question=null` 时，不再回填工具草稿；缺省字段的兼容路径仍存在，出题和判分语义需另外验收。
- 多模态启用时，PDF 图片工具的本地渲染结果实际加入模型消息，不能仅传图片数量。公开 trace 保留元数据，不包含原图。
- 场景工具返回后续操作所需的应用场景/物品 ID，并保留父子和所属关系。场景与物品列表分别最多 128 项，截断显式标记，provider 字节预算继续执行；公开 trace 仍脱敏，模型不能生成应用身份。
- 检索记忆保留领域来源、说话者与原始记录时间。学生原文摘录最多 800 字符、助手最多 160 字符，避免助手复述挤掉学习者更新。
- 原文选择器按查询选择句子邻域、多处窗口及偏移，在总计 800 字符预算内保留证据。这是原文检索，不是模型摘要压缩；远处限定、长句和否定关系仍可能丢失。
- 词法记忆检索回退使用稳定散列和 Unicode/重音规范化，支持中文 bigram，避免 Python hash seed 改变排序。这不等于语义检索或时间关系正确。
- 自动教材引用不再为无文档、无 chunk 或零词法匹配编造第 1 页/单元范围。仍保留匹配 chunk 排序与去重；词法命中不能证明蕴含，中文召回和跨语言误引仍在 [质量 TODO](quality/TODO.md) 中。

## Provider、Persona 与 Document

- M3 完整工具调用信封中的有界非负整数 `index` 可在 SDK 适配层去除；保留原 ID、顺序与参数。非法索引、不完整调用、额外身份或未知字段仍交严格解码器处理，不拼装流式片段，不从索引分配身份。
- Chat Completion 关闭 SDK 内部重试，由 ProviderTransport 统一管理，避免 3 次传输尝试扩大为 9 次 HTTP 请求；未据此认证 Responses/Embedding 路径。
- 每次 Provider 尝试前检查有效期限；已发出的同步请求仍受其传输超时限制，不能宣称可强制取消上游执行。
- 工具错误保留安全的类型化错误码与修复路径，预算拒绝显示对应诊断分类；不泄漏原始敏感载荷。
- Persona 嵌套卡片严格 schema 校验进入既有恢复预算，Chat/Responses 每个传输最多两次调用，JSON 与嵌套校验共享额度。不能补造字段或应用 ID；policy 为 `persona-generation-harness-v2`。
- OCR 仅在有实际引擎证据时报告识别语言，配置语言不冒充检测结果。macOS Vision 仍是实验工具，未接入生产 OCR。

## Tavern 本地上下文处理

长历史 preflight 减少重复序列化，在原预算内寻找可保留的历史后缀，保留全量快路径、空历史错误优先级和最终上下文。预算、Prompt 和事实保留策略未改变。

30 组/长度、共 90 组旧新配对的 messages、retained messages 与 report 完全相等。256 条消息的本地 P50/P95 从 631.49/655.47 ms 降为 41.86/43.43 ms。它是本地预算处理收益，不能推导缓存、模型延迟或早期事实召回改善。[稳定测量基线](quality/evidence/tavern-preflight-comparison-v1.json)保留用于性能回归。

## 采用记录

提交可用 `git show <commit>` 查看；过程日志不是当前行为真源。

| 提交 | 已采用范围 |
| --- | --- |
| `276783c` | Planning 同轮 3 次详情、总计 4 次及共享预算契约 |
| `daa04c8` | 修订单元范围内的详情证据 |
| `991b396` | 页文本字符与分隔符预算 |
| `1b0501d` | 分配前限制图片页范围 |
| `49af44c` | 工具回执批次完成后附图 |
| `8fd3222` | 严格修复保留工具证据 |
| `ba2afc6` | 类型化页范围恢复指引 |
| `4e72e53` | 修复/提交共用章节不变量 |
| `1879b51` | 修复提示的时间戳/活动时长区分 |
| `968af23` | 估分去掉少单元的虚假跨度扣分 |
| `4c9ee6d` | 初始提示和运行循环的短源分类 |
| `1318b06` | 估分结果和初始提示的能力边界 |
| `98cb5dc` | 估分目录、标签与共享契约对齐 |
| `836f2c8` | 预算拒绝的诊断摘要 |
| `a842706` | 互动题 Markdown 空白 |
| `f7fccff` | 显式取消题目优先于工具草稿 |
| `3f97261` | Study 实际接收 PDF 图片 |
| `f0ffb47` | 场景操作目标身份投影 |
| `f0e2b56` | 记忆领域来源 |
| `4541e53` | 说话者与独立摘录预算 |
| `6ee07eb` | 来源记录时间 |
| `c12dbe6` | 稳定多语言词法检索 |
| `53651fa` | 有总预算的查询相关原文窗口 |
| `7102e74` | 移除无匹配依据的兜底引用 |
| `bfab1ca` | 完整工具信封 index 兼容 |
| `9dd6447` | Chat 重试归传输层管理 |
| `7c1690d` | Provider 尝试前期限检查 |
| `39573b8` | 安全的工具错误和修复路径 |
| `c8f5b43` | Persona 嵌套 schema 有界修复 |
| `0b721a5` | OCR 语言证据归属 |
| `acbc90a` | Tavern 本地长历史裁剪成本 |

## 验证范围

最后一个生产修复 `7102e74` 后，850 项完整后端测试与 116 项定向测试通过；`deed0ae` 仅追加诊断，没有后续生产修改。共享契约测试在估分目录同步时通过。起始版本的 14 套确定性 Harness eval 通过，不是清理时重新执行的全量发布验收。

本次文档整理不运行真实模型。需要代码回归时使用仓库规定的 `npm run test:ai`、`npm run test:contracts`；发布门使用 `npm run check:release`。没有新的独立专家、浏览器或跨平台认证。
