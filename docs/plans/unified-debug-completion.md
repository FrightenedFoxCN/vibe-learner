# 统一 Logging 与 Debug 最终归档

2026-09-10 完成四阶段实施、逐项核对及分步提交。最终实现提交为
`b271536`，完整证据见[验收矩阵](unified-debug-acceptance.md)，历史记录见
[实施计划](unified-debug.md)。此归档属于开发者实施验收；用户暂缓的独立质量
复核仍由根 TODO 跟踪。

| 任务 | 最终结果与证据 |
| --- | --- |
| OBS-FOUNDATION-001 | 版本化安全事件、跨端关联、独立有界存储；Persona 纵向链、共享契约与故障隔离验证。 |
| OBS-FLOWS-001 | Document/OCR/Planning、Persona/Scene、Study/题目/附件、Tavern、Settings/Vault、导入导出与桌面生命周期；真实 canonical 引用、provider/tool 指标及重启补建。各成功/异常链证据逐项列于验收矩阵。 |
| OBS-DEBUG-001 | 独立 Provider、页面快照归属、按需页面/全局查询、筛选及关联展开；StrictMode、路由/迟到响应和实际浏览器验证。 |
| OBS-AUDIT-001 | 白名单导出、原始指标与分组统计、有界留存/回收/恢复、采集缺口、故障注入及本机性能门；浏览器与原生保存读回验证。 |

最终 `check:release` 通过：745 项后端测试、共享/Web reliability 与类型门、
13 组 Harness suite、生产 Web 构建。另有 16 项真实 Chromium 场景、22 项原生
测试、20 项 Python 存储专项和正常原生构建通过。日志位置及原始报告均在验收矩阵
与各性能报告中列出；归档 JSON 保留样本、环境及源码摘要，不依赖临时日志保存结果。

六组实际 mock 后端请求链的诊断增量 P95 均低于原定 50ms。实际 React 在 Chromium
及 macOS Tauri WebView 的普通/复杂事件量测通过。最后一个失败门经生产计数协议
优化解决：原生满载写入开发/优化 P95 为 20.03/20.07ms，低于原定 25ms。
原先 25.21/25.06ms 失败、Scene GC 归因及未采用的同步实验均保留。
详见[计数优化报告](../performance/diagnostic-native-journal-v1.md)。

主要收尾提交：`c758e5c` 修复开发构建 Vault 解锁慢（约 46 秒降至 0.97 秒，
原因为密码派生依赖未优化，并非数据库表）；`22e0d4f` 修复设置 Debug 凭据投影；
`a80cdf9` 修复原生清空凭据权限；`3a43527` 补齐完整导入动作；
`aa24e58` 完成原生 WebView 量测；`b271536` 接入持久化双槽计数并通过最终集成门。
各业务性能提交、失败实验及更早阶段可由计划记录和 Git 历史追溯。

**升级/降级限制：** 新计数格式兼容旧数据迁移；迁移后旧版原生程序拒绝继续写
诊断日志，业务仍与日志故障隔离。不要手工重置计数文件以绕过拒绝。
详见[原生计数协议](../diagnostic-native-counter.md)。

存储正常分区预算合计 196MiB；200MiB 是参考值，未知文件和最多 512MiB 的恢复
工作空间是明确例外，不承诺安装目录硬上限或完整七天历史。采集丢失与无法观察
部分继续显式呈现。此次未发起真实 MiniMax 调用，不认证模型质量、真实上游费用、
其他平台或任意断电安全；独立复核仍暂缓。密钥不属于诊断或归档内容。
