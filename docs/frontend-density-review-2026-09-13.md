# 前端密度重构记录（2026-09-13）

本轮围绕 TODO 的三个 UX-DENSITY 项，保留现有浅灰面板、边框、青色强调色与响应式导航。未修改后端契约、模型请求或用户数据。

- Sensory Tools：搜索名称/用途/分类；按阶段、可用性筛选；结果计数及空态；工具用途默认折叠。批量管理独立开启，仅更改筛选结果中的可用工具。单项保存保留焦点，提示有稳定占位，复选框使用 18px 视觉尺寸和 44px 点击区域。
- Persona Spectrum：卡片类型与人格来源筛选；批量清空确认；库删除折叠到独立入口。生成预览展示字段前后值与清空影响，可单卡合并。整批应用成功且名称缺失时定位名称；取消替换不移动焦点。
- Scene Setup：已保存场景搜索、节点类型筛选、库删除独立入口。生成预览比较层级路径并列出规则、物体及交互；重命名/移动按移除与新增展示，语义冲突仍需人工检查。
- 异步错误反馈继续遵守原焦点所有权规则，聚焦错误时使用 `preventScroll`，避免强制滚回页顶。

## 验证范围

`npm run check` 和生产 Web 构建通过；Playwright 使用隔离合成 API 响应，覆盖筛选、批量写入范围、保存成功/失败、焦点、390px 横向边界，以及既有 Persona 生成输入、Scene 草稿恢复和删除模态。截图人工检查沿用现有风格。未调用真实模型。

浏览器回归发现并修复首次保存时提示从无到有引起的 44px 滚动跳动。此记录属于实施者自测；TODO 保留独立 UX、真实设备及未实现的提案审核能力，不将合成数据成功视为完整验收。
