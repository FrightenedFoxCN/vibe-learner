# Project handoff — 2026-08-13

## Stop point

Development intentionally stops after the `UX-STUDY-ATTEMPT-001` batch is committed and fast-forwarded to local `main`. Do not start another feature in the same commit. The active development branch is `codex/tavern-harness`; local `main` is the integration branch. No remote push is part of this handoff.

One pre-existing isolation stash must remain untouched unless its owner explicitly resumes it:

```text
stash@{0}: On codex/tavern-harness: agent-frontend-perf-isolation-wip
```

## Delivered slices

The recent single-feature commits, in order, are:

```text
8ee07d5 fix(study): make session writes revision-safe
0df604e feat(study): add durable chat operation admission
1a6bdde fix(study): recover pre-admission chat failures
4d2e654 feat(harness): prepare study plan confirmation effects
3a3bc4b feat(tavern): surface scoped run recovery
```

The final Study attempt UX commit is created in this handoff turn after the checks below and must be fast-forwarded to local `main` before stopping.

### Study Chat admission and recovery

- Study Chat POSTs use a stable client request identity and expected Session revision.
- The backend durably admits one operation before provider execution and exposes query-only terminal recovery.
- The browser distinguishes explicit pre-admission 4xx failures from ambiguous transport/provider/decode failures, preserves the learner draft, and never blindly replays an uncertain operation.
- Session Turn, Session revision, committed response, and operation receipt share the final database transaction.

This does not prove model tool, file, generated artifact, or provider effects exactly once.

### First typed Study database effect

- `update_learning_plan` and `update_learning_plan_progress` now create strict model-suggestible proposals rather than immediately mutating Session confirmations.
- The application assigns operation/batch/effect/confirmation identity and binds both Study Session and Learning Plan targets.
- Plan schedule references are checked against the Session-bound Plan snapshot.
- Plan confirmations commit with the validated final Turn, Session revision, and operation receipt.

The prepared batch is in-process only. It is not a durable effect journal, public effect receipt, v3 Harness runtime, or provider exactly-once proof. Parent tasks `SCH-HRN-EFFECT-001`, `STUDY-EFFECT-COMMIT-001`, and `HRN-STUDY-001` remain open.

### Tavern scoped recovery visibility

- The latest eligible facilitated partial/failed run in the loaded history is visible in `Interaction Composer`.
- The callout names failed/blocked personas and states that committed replies will not be regenerated.
- It invokes the existing scoped child retry and does not use or mutate the current composer draft, guidance, or target selection.
- Archive/other mutations, pending runs, and older-history loading disable recovery.

This is a loaded-window UX slice only. `TAV-RUN-VIEW-001`, `TAV-ERROR-001`, `TAV-RECOVERY-CLIENT-001`, and the parent `UX-TAV-REC-001` remain open.

### Interactive-question submission UX

- Each Turn has an independent local `submitting` / `failed` state.
- During submit, answer editing, duplicate submit, and “ask another question” are disabled.
- No local verdict is displayed or cached before persistence.
- Failure preserves the selected/typed answer and exposes an in-question retry without revealing a verdict.
- Success is displayed only after the returned Session advances revision and binds the same Session, Turn, submitted answer, boolean verdict, and non-empty feedback.
- The optional automatic callback runs after answer commit; callback failure does not roll back or visually downgrade the recorded answer.

The backend contract is deliberately not redesigned in this UX slice. Current known risks are fully tracked by `SCH-STUDY-QUESTION-001` and `SCH-STUDY-ATTEMPT-001`: answer material is exposed before submit, the endpoint locates a question by prompt, accepts client grading fields, and lacks Turn/revision/stable-attempt identity.

## Final independent audit additions

The closing independent browser/schema audit made no change to the delivered slice and found no blocker for its commit. It added precise acceptance detail to existing open TODOs:

- `UX-TAV-MOBILE-001`: at 390×844 with no Room, the empty transcript and disabled Composer consume the first viewport while Session/Setup are far below it; reorder the no-Room DOM, hide or downgrade dead controls, and make “Create Tavern” scroll/focus the title. The same audit reconfirmed `UX-TAV-STATE-001`, `UX-TAV-REC-BADGE-001`, `UX-NAV-001`, and `UX-EMPTY-001` remain open.
- `SCH-HRN-EFFECT-DB-NEXT-001`: memory, affinity, and the existing plan-confirmation proposal must share one discriminated, globally slotted mixed batch with prepared-state read-your-writes and one transactional validation/commit boundary. Do not split batches by effect type or expose prepared tools as saved.

These are handoff findings, not part of the final implementation commit's functional scope.

## Verification at handoff

The final Study attempt UX batch passed:

```text
npm run test:web:reliability
50 passed, 1 optional live-backend case skipped, 0 failed

npm run build:web
Next.js production build passed; all 13 static pages generated

git diff --check
passed
```

The immediately preceding Tavern recovery batch passed:

```text
npm run test:web:tavern
23 passed, 1 optional live-backend case skipped, 0 failed

npm run build:web
passed
```

The typed plan-confirmation effect batch passed 15 targeted backend tests, Python compilation, shared TypeScript contracts/golden registry, and independent schema ownership review.

`MODULE_TYPELESS_PACKAGE_JSON` warnings from direct TypeScript test execution are non-blocking and tracked by `QG-WEB-ESM-001`.

## Recommended next work

Follow the ordered section at the top of `TODO.md`. The best next functional/schema slice is `SCH-STUDY-QUESTION-001`, because unsubmitted question payloads currently expose grading material. Keep it separate from the subsequent attempt-operation migration.

For every future slice:

1. Read `AGENTS.md`, `TODO.md`, `docs/harness-engineering.md`, and `docs/harness-schema-ownership.md`.
2. Use an independent agent for UX or schema ownership acceptance; the implementing agent must not self-close the finding.
3. Keep one feature per commit and run only proportional, targeted gates unless a serious defect appears.
4. Record secondary defects in `TODO.md` instead of expanding the active slice.
5. Fast-forward the verified commit to local `main`, return to `codex/tavern-harness`, do not push without explicit authorization, and do not alter the isolation stash.
