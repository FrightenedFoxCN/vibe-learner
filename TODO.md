# TODO

## 用户体验感知

- [x] 更新使用文档；
- [x] 多模态功能测试，聊天窗的多模态支持；
- [-] 增添更复杂的实例人格和场景；
- [x] 增加更多模型的接口；

## 计划创建模块

- [ ] 允许模型逐步修改计划，而非一次性完成生成；
- [ ] 压缩计划生成的轮次，鼓励模型进行并行的工具调用；

## 对话优化

- [x] 章节对话增加随时间自己回复的功能：增加一个 tool call 允许模型触发在何时间后自动唤起，触发一个隐藏的唤醒回复并继续；
- [x] 对话过程中进行计划的修改、完成度标记（需要用户确认）；
- [x] 对话前先触发一轮提示词，进行场景预处理和引入；
- [x] 增加系统时间读取 tool call；
- [x] 增加 memory 数据临时存取和 tool call；
- [x] 增加好感度机制和相关 tool call；
- [x] 观察审计 RAG 数据调用；
- [x] 增加对投射的 pdf 的更多指示：指示某段文字或某个页面的某个坐标，支持高亮或框选的 tool call；
- [x] 删掉对话审计与会话状态：将好感度和当前投射 pdf 等有用信息拉到人格面板可视化，不重要的数据全部丢进 debug；
- [x] 人格面板下移一点，不被收起的 pdf 浮窗遮挡；
- [x] 允许多模态模型投出用户上传的图片并在上面进行绘制和标注；
- [ ] 对于可生成图片的模型，允许模型在预览框中投出生成的图片；

## 数据流优化

- [ ] 数据库结构重建，迁移到 pgsql；
- [ ] 数据层主结构清理，分离出不同数据结构的 CRUD api；
- [ ] 对缓存数据和临时记录进行统一管理，允许用户（基于不同入口对不同层级的数据）进行清理；
- [ ] 相关文档完善；
- [ ] 切换页面仍保留原始页面状态，不中断调用；刷新前页面状态缓存；
- [ ] 提高对上游模型请求处理的稳定性，扩增重传兜底机制，但保证错误透明；

## 未来优化

- [ ] 封包：跨平台桌面分发方案见 `docs/desktop-distribution-plan.md`，当前延后处理；
- [ ] Rust 重写：见新分支；
- [ ] 进一步完成性能优化；
- [ ] 根据人格和场景设置进行 ui 的动态调整，更多 ui 主题；
- [ ] Live2D 支持和 TTS：这个很麻烦，以后再说；
- [ ] 前端重构：现在这个单页面模式非常愁人，史山越堆越高了；
- [ ] 蒸馏有用的 skills.md 并搭载；
