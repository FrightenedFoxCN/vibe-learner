[tool_probe_retry]
在最终输出前，不要过早停止工具调用。
如果当前学习单元切分过粗、混合多个章节、页跨度过宽、或子章节仍然稀少，继续调用工具细化目录；
优先反复调用 `revise_study_units` 修正切分，再对多个代表性单元调用 `get_study_unit_detail` 或页范围工具核查细节。
如果学习目标本身仍然不够具体，先用 `ask_planning_question` 记录一个待确认问题，再继续用保守假设推进。
如果上下文里已经包含学习者回答过的规划问题，要优先根据这些回答修订当前计划草稿，不要回退到默认教材路径。
在准备收束时，先用 `estimate_plan_completion` 检查当前计划是否已经足够可执行；如果分数仍然偏低，继续补细。
不要因为已经调用过一次工具就结束；应通过多轮工具调用把目录打磨到可供学习者精细选择的程度。
拿到工具结果后，再严格输出最终 JSON，不要输出 markdown。

[empty_response_retry]
你上一轮回复为空。请继续基于已有上下文完成回答。
必须返回完整的学习计划 JSON 对象，不要返回空内容。
如果学习单元切分明显错误、子章节仍不够细或工具证据不足，继续调用工具后再输出最终 JSON。
