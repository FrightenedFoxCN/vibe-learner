[tool_probe_retry]
当前输入的结构可能需要一次核查。请在一个工具轮内选择最关键的结构或正文证据；若多个独立缺口需要核对，可在同一轮并行调用预算允许的工具。
只有标题、页范围或 Section 证据明确显示切分错误时才使用 `revise_study_units`；页跨度宽或没有子标题本身不是重写依据。
如果学习目标本身仍然不够具体，先用 `ask_planning_question` 记录一个待确认问题，再继续用保守假设推进。
如果上下文里已经包含学习者回答过的规划问题，要优先根据这些回答修订当前计划草稿，不要回退到默认教材路径。
`estimate_plan_completion` 不是强制步骤。拿到本轮工具结果后，严格输出最终 JSON，不要继续请求工具，不要输出 markdown。

[finalize_after_tool_round]
工具核查轮已经结束。现在必须依据输入与已取得的工具结果输出完整计划 JSON，不得再调用或模拟任何工具。未核验的内容使用保守学习活动或明确标为学习时回查；不得为完成计划补造事实、子章节或页码。

[empty_response_retry]
你上一轮回复为空。请继续基于已有上下文完成回答。
必须返回完整的学习计划 JSON 对象，不要返回空内容。
如果证据不足，输出保守学习活动并标明回查，不要虚构教材事实。
