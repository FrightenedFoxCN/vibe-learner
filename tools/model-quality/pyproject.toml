[project]
name = "model-quality-lab"
version = "0.1.0"
description = "Process-isolated model experiment runner with durable wire accounting"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.11,<3", "certifi>=2026.2.25"]

[tool.uv]
package = false
